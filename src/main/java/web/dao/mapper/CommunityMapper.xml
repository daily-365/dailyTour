<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  
  <mapper namespace="web.dao.face.CommunityDAO">
  
  <select id="communityList">
  
   select c.communityNo,m.userId,c.communityTitle,c.communityDate,c.communityVisit
   from community c
   inner join member m
   on c.userId = m.userId
   
   
   
  </select>
 
   
     
  
  </mapper>