<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

  <mapper namespace="web.dao.face.MemberDAO">
  
   <!-- 회원가입 -->
   <insert id="join">
   insert into tbl_member (userId,userPw,userNick,userBirth1,userBirth2,userBirth3,userGender)
   values (#{userId},#{userPw},#{userNick},#{userBirth1},#{userBirth2},#{userBirth3},#{userGender})
   
   </insert>
  
  <!-- 로그인 -->
  <select id="login" resultType="int" >
   
    select count(*) 
    from tbl_member
    where userId=#{userId}
    and userPw= #{userPw}
     
  
  </select>
  
  
  </mapper>